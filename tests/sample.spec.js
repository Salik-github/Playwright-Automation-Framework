// import {test,expect} from './pomS/fixture';

// test('should load the homepage',async ({page, mainUrl})=>{
//     await page.getByRole('spinbutton',{name:'Input: Number'}).fill('123');
//     await page.getByLabel('Input: Text').fill('Hello, Playwright!');

//     await expect(page.getByText('Display Inputs')).toBeVisible();
//     await expect(page).toHaveURL('https://practice.expandtesting.com/inputs')
//     await expect(page.getByRole('spinbutton',{name:'Input: Number'})).toHaveValue('123')
//     await expect(page).toHaveTitle('Web inputs page for Automation Testing Practice')
//     await expect(page.locator('.input-box')).toHaveCount(4);
//     await page.getByText('Home').click();
//     await expect(page).toHaveURL('https://practice.expandtesting.com');
// });
// /// import {page} from '@playwright/test'; 
